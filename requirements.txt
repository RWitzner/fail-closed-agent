# Tests are stdlib-only and require NO third-party deps (the offline suite runs on a
# bare checkout with no install). Third-party deps are pinned to exact versions in the
# milestone that first imports them; each is imported LAZILY (never at module scope) so
# the offline no-network/no-creds suite stays green without them.
#   M1 tier-2 (entitlement verify + live transport): the databento SDK.
#   M2 (session/calendar gate): exchange_calendars — the live ScheduleProvider
#     (implemented 2026-07-02: lazy import inside _build_calendar only, version-pin
#     enforced fail-closed; offline tests drive it via a sys.modules-injected fake).
#   M5 (paper broker) + the paper-account verifier: alpaca-py — lazy import inside
#     AlpacaPaperBroker._build_real_client / verify_alpaca_paper._build_real_client only.
databento==0.79.0
exchange_calendars==4.13.2
alpaca-py==0.43.5
